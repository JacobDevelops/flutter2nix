/**
 * TapiShim: stub implementation.
 *
 * Phase 1: connect to Gradle daemon via Tooling API, enumerate resolved
 * project + buildscript dependencies, write JSON to stdout.
 */
fun main(args: Array<String>) {
    System.err.println("TapiShim: stub — not yet implemented (see Phase 1)")
    System.exit(1)
}
