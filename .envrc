# flutter2nix dev environment is defined by devenv (devenv.nix / devenv.yaml).
# Requires devenv — install once with: nix profile install nixpkgs#devenv
# See https://devenv.sh/getting-started/
eval "$(devenv direnvrc)"
use devenv

[[ -f .envrc.local ]] && source_env .envrc.local
