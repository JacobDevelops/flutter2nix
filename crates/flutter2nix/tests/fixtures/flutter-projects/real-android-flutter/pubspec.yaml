name: real_android_flutter
description: Flutter project fixture for flutter2nix Level-2 E2E tests (real TAPI, real network).
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
